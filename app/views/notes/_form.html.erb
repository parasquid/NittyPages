<%= form_with model: @note do |form| %>
  <div>
    <%= form.label :body %><br>
    <%= form.text_area :body %>
  </div>

  <%= form.hidden_field :date %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
